# Usage
- uses `./run_rebalance` to run the `rebalance.native` script.

# Installation
- Add cronjob:

        */5 * * * * $PWD/run_rebalance

# Notes
- started on Oct. 20th w/ 1.0 share of each stock. 
